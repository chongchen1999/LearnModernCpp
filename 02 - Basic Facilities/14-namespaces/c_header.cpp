#include <cstdio>  // Include C standard I/O
using std::printf;  // Bring only printf into the global namespace

int main() {
    printf("Hello, world!\n");  // Works fine now, using printf from std
    return 0;
}